/*
 * @ldlework/phosphor
 *
 * Primitives are the public API. The CSS is delivered separately as
 * `@ldlework/phosphor/styles.css` and must be imported once by the
 * host app before any of these components will render correctly.
 */
import './styles/index.css'

export * from './primitives'
